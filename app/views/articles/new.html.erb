<%= provide(:title, 'Article') %>

<div class="article-form-center">
  <h1>New Article</h1>

  <%= render 'form', article: @article %>

  <p class="mt-3">
    <%= link_to 'Back', articles_path %>
  </p>
</div>
